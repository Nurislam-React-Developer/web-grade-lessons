// theme.js
import styled from 'styled-components';

// export const theme = {
// 	primaryColor: 'green', // или какой-то дефолт
// 	borderRadius: '5px',
// };

// // Компонент
// export const Box = styled.div`
// 	color: ${({ primary, theme }) => (primary ? 'darkblue' : theme.primaryColor)};
// 	border-radius: ${({ theme }) => theme.borderRadius};
// `;

export const lightTheme = {
	primaryColor: 'green',
	borderRadius: '5px',
};

export const darkTheme = {
	primaryColor: 'darkblue',
	borderRadius: '5px',
};
